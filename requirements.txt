# Transformador Fonético Español - Dependencias
# Optimizado para RTX 4080 SUPER con Ollama

# Core dependencies
ollama>=0.3.0
psutil>=5.9.0

# Optional GPU monitoring (instalar si está disponible)
# pynvml>=11.0.0  # Descomenta si quieres monitoreo avanzado de GPU

# Data handling
dataclasses-json>=0.6.0

# Optional async processing
asyncio-throttle>=1.0.0

# Development and testing (opcional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0